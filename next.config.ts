import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: "export",
  // If you decide to use the <Image /> component later, you'll also need this:
  // images: { unoptimized: true } 
};

export default nextConfig;